# test 
to have a test report in .html - start pytest with parametrise:
pytest --html=report.html --self-contained-html

add parameter --browser=Firefox to start test in Firefox. Default browser is Chrome.

